rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /routineSync/{docId} {
      allow read, write: if docId == "d7CKIRH_JFyWyMzAgG9sLD0nHw_P5yFn";
    }
  }
}
